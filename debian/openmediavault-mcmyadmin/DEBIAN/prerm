#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/mcmyadmin" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d mcmyadmin stop || exit $?
	else
		/etc/init.d/mcmyadmin stop || exit $?
	fi
fi
# End automatically added section
